

1) create new branch  <-- create new branch -->
    - git branch -c <new_branch_name>
    
2) switch to newly created Branch 
   -git checkout <branch_name>
   
3) git branch <-- check the branch -->
   - check the branch status
   
4) git add <-- add file -->
   - add the file those are changes
   
5) git status <-- check the status -->
  - check the staus has been changed 
  
5) git commit -m "changes message here"
  - commit the file those are has been commited

6) git push  origin <sachin>
   - to upload the file created the newly created  branch 
   
  
  
